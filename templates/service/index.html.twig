{% extends 'base.html.twig' %}

{% block title %}Hello ServiceController!{% endblock %}

{% block body %}

{% if formAddService|default %}

{{ form_start(formAddService, {'attr': {'class': 'ui form'}}) }}
{# 
{{ form_row(form.theme) }}
{{ form_row(form.category) }} #}

<div class="field">
    {{ form_row(formAddService.title) }}
</div>
<div class="field">
    {{ form_row(formAddService.description) }}
</div>
{# traitement en interne mais non  #}

{# <div class="field hidden-field">
    {{ form_row(formAddService.user) }}
</div> #}

<div class="field">
    {{ form_row(formAddService.price) }}
</div>
<div class="field">
    {{ form_row(formAddService.duration) }}
</div>
{# traitement en interne mais non  #}
{# <div class="field hidden-field">
    {{ form_row(formAddService.createDate) }}
</div> #}

<div class="field">
    {# {{ form_row(formAddService.course) }} #}
</div>
<div class="field">
    {{ form_row(formAddService.Valider) }}
</div>

{{ form_end(formAddService) }}
{% endif %}

{% if services | default %}
{% for service in services %}
{{ service }}
{% endfor %}
{% endif %}

{% endblock %}