{% block pending_orders %}
    {% if orders %}
        <div class="ui relaxed divided list">
            {% for order in orders %}
                <div class="item">
                    <i class="large middle aligned history icon"></i>
                    <div class="content">
                        <a class="header">{{ order.title }}</a>
                        <div class="description">{{ order.dateOrder|date("d/m/Y H:i") }}</div>
                    </div>
                </div>
            {% endfor %}
        </div>

        {% if pagination.pageCount > 1 %}
            <div class="ui pagination menu centered custom-margin-top">
                {% for page in range(1, pagination.pageCount) %}
                    <a class="item{% if page == pagination.currentPageNumber %} active{% endif %}"
                       href="{{ path('profile_pagination', {'id' : app.user.id, 'status': 'pending', 'page': page }) }}">
                        {{ page }}
                    </a>
                {% endfor %}
            </div>
        {% endif %}
    {% else %}
        <p>No pending orders found.</p>
    {% endif %}
{% endblock %}

{% block completed_orders %}
    {% if orders %}
        <div class="ui relaxed divided list">
            {% for order in orders %}
                <div class="item">
                    <i class="large middle aligned history icon"></i>
                    <div class="content">
                        <a class="header">{{ order.title }}</a>
                        <div class="description">{{ order.dateOrder|date("d/m/Y H:i") }}</div>
                    </div>
                </div>
            {% endfor %}
        </div>

        {% if pagination.pageCount > 1 %}
            <div class="ui pagination menu centered custom-margin-top">
                {% for page in range(1, pagination.pageCount) %}
                    <a class="item{% if page == pagination.currentPageNumber %} active{% endif %}"
                       href="{{ path('profile_pagination', {'id' : app.user.id, 'status': 'completed', 'page': page }) }}">
                        {{ page }}
                    </a>
                {% endfor %}
            </div>
        {% endif %}
    {% else %}
        <p>No completed orders found.</p>
    {% endif %}
{% endblock %}
